using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class onveyor : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // FindStart Postition
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
